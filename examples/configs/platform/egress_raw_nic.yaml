# Example egress configuration: send matched packets out a specific NIC
# via AF_PACKET, bypassing the local routing table. Use this when you
# want forwarded traffic to leave a named physical port regardless of
# what /proc/net/route says.
egress:
  kind: raw_nic
  device: ens5f0np0
  raw_nic:
    bind_device: true
